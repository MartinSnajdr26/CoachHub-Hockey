# Application environment
# development | production
APP_ENV=development

# Flask secret key (generate a strong random value)
SECRET_KEY=change_me_in_production

# Database (SQLite example; path is resolved relative to project root)
DB_URL=sqlite:///data/dev.sqlite3

# Mail settings (configure your SMTP to send verification/reset emails)
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASSWORD=your_smtp_password
MAIL_SENDER=martinsnajdr@coachhubhockey.com

# Token lifetimes
# Email verification link validity (seconds)
EMAIL_TOKEN_MAX_AGE=172800
# Password reset link validity (seconds)
PASSWORD_RESET_TOKEN_MAX_AGE=3600

